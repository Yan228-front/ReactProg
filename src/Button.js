import React from "react"

const Button = (props) => {

    let onClick = () => {
      
    }

    return(
        <div>
            <input type={"text"} ></input>
            <button>add</button>
        </div>
    )
    
     
}

export default Button;